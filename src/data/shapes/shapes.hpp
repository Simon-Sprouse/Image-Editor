#pragma once

#include "../image/image.hpp"

using namespace image;

namespace shapes {
    
    struct Rect { 
        Point tl;
        int dx;
        int dy;
    };

    // todo overload operators on rect like comp and equals


}